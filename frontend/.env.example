# Browser-visible API base URL. In Docker, nginx proxies this path to the API.
VITE_API_BASE_URL=/api

# Used only by the Vite development server proxy; never expose a secret here.
VITE_API_PROXY_TARGET=http://localhost:8080
VITE_PORT=5173
